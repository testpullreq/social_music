<div class="form-group">
    <p><%= display_name %></p>
    <% for (var i = 0; i < values.length; ++i) { %>
        <div class="checkbox">
            <label><input type="checkbox" name="<%= name %>" value="<%= values[i].value %>"><%= values[i].label %></label>
        </div>
    <% } %>
</div>